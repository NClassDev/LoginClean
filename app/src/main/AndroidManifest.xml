<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.loginclean">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:name=".LoginCleanApp"
        android:theme="@style/Theme.LoginClean">
        <activity android:name=".presentation.passwordrecover.view.PasswordRecoverActivity"></activity>
        <activity android:name=".presentation.register.view.RegisterActivity" />
        <activity
            android:name=".presentation.main.view.HomeActivity"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.LoginClean.NoActionBar" />
        <activity android:name=".presentation.login.view.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>